machine:
    services:
        - docker

dependencies:
    override:
        # Build containers
        - docker build -t sjevs/docker-terraform .

test:
    override:
        - ./runTests.sh

deployment:
    docker:
        branch: ["master"]
        commands:
            - docker push sjevs/docker-terraform
